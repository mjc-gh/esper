extern crate esper;
extern crate hyper;

#[cfg(test)]
mod tests {
    // TODO
    // Figure out how to "fake" a Control instance so we can test
    // Manager's methods (subscribe, unsubscribe, publish and etc.)

    //use esper::{Manager, Client, Topic};
    //use hyper::Control;
}
